# 影像製作所平台 — 功能說明文件

> **影像製作所**是一個以電影心得交流為核心的社群平台，讓使用者能分享觀影心得、搜尋電影評論、發起觀影揪團活動。  
> 本文件將平台功能分為三大區塊進行說明。

---

## 一、使用者系統（帳號與身分管理）

平台支援**兩種身分**的使用者註冊與登入，採用 JWT (JSON Web Token) 進行身分驗證。

### 1.1 校內使用者（教職員 / 學生）

| 欄位 | 說明 |
|------|------|
| 校園 ID（學號） | 9 位數字，作為登入帳號與 Primary Key |
| 真實姓名 | 存於機密身分表 `UserIdentity`，僅管理員可見 |
| 科系 | 存於機密身分表 |
| 學校信箱 | 必須為 `@cc.ncu.edu.tw` 結尾 |
| 公開暱稱 | 對外顯示的代碼，全站唯一 |
| 密碼 | 經 Django 密碼驗證器檢驗 |

### 1.2 校外使用者

| 欄位 | 說明 |
|------|------|
| 帳號 ID | 系統自動產生 9 碼英文字母 ID（26 進位） |
| 真實姓名 | 存於 `OutsiderIdentity` 表 |
| 聯絡信箱 | 作為登入帳號使用 |
| 職業 | 註冊時填寫 |
| 公開暱稱 | 對外顯示的代碼，全站唯一 |

### 1.3 登入 / 登出

- 校內使用者以**學號 + 密碼**登入
- 校外使用者以**信箱 + 密碼**登入（後端自動將信箱轉換為對應的帳號 ID）
- 登入後取得 JWT Access Token 與 Refresh Token
- 支援 Token 自動刷新機制

### 1.4 個人主頁

- 顯示使用者基本資訊（暱稱、真實姓名、科系、校園 ID）
- 統計數據：已發布心得數、獲得推薦總數
- **我的觀影心得**：列出自己發布的所有心得，可點擊進入詳情進行編輯或刪除
- **我留言過的心得**：列出自己留言過的他人心得，方便回顧

---

## 二、核心功能（心得、搜尋、活動）

### 2.1 電影心得系統

#### 發布心得
- 填寫**電影名稱**（系統自動建立 Movie 資料，使用 `get_or_create`）
- 撰寫**心得內容**（支援多行文字）
- 選擇**推薦指數**（1–5 星互動式評分）
- 添加 **Hashtag 標籤**（以 `#` 開頭、`;` 分隔，電影名稱自動加入標籤）

#### 瀏覽心得（首頁）
- 首頁顯示**熱門排行榜**（依推薦人數排序，取前 10 篇）
- 採用 10 分鐘快取機制提升效能，同時動態計算每位使用者的按讚狀態
- 每篇心得卡片顯示：電影名稱、內容摘要（2 行截斷）、標籤、推薦指數、作者、留言數、推薦按鈕

#### 心得詳情 Modal
- 完整顯示心得內容（支援換行顯示）
- 推薦（按讚）功能：點擊切換推薦/取消推薦
- **留言區**：登入使用者可發表留言，即時顯示留言列表
- **編輯功能**：作者可修改電影名稱、心得內容、推薦指數、標籤
- **刪除功能**：作者可軟刪除自己的心得（不會永久移除，管理員可復原）

#### 投票（推薦）機制
- 每位使用者對每篇心得只能投一票
- 重複點擊會取消推薦
- 資料庫層級使用 `unique_together` 防止重複投票

### 2.2 電影心得搜尋

- 提供全站搜尋功能，支援**電影名稱**與**心得內容**的模糊比對
- 搜尋結果依**相關度排序**（電影名稱匹配優先於內容匹配）
- 搜尋結果頁面與首頁一致的互動體驗：可點擊查看詳情、推薦、留言

### 2.3 電影迷活動板

#### 發起活動
登入使用者可發起揪團活動，需填寫以下欄位：

| 欄位 | 說明 |
|------|------|
| 活動標題 | 例如：「週末一起看《奧本海默》」 |
| 放映時間 | 必須為未來的時間（後端驗證） |
| 地點 | 不可為空白 |
| 主辦人代稱 | 不可為空白 |
| 活動描述 | 選填，詳細說明 |

#### 瀏覽活動
- 活動列表依**放映時間**排序
- 每張活動卡片顯示：標題、時間、地點、描述、發起人

### 2.4 響應式設計

- **桌面版**：頂部導航列顯示所有頁面連結與使用者操作
- **手機版**：
  - 頂部導航列精簡為品牌 Logo + 漢堡選單（個人主頁、登出等）
  - 底部固定 Tab Bar 提供三個核心入口：**心得**、**搜尋畫面**、**建立活動**
  - Active Tab 有紫色指示條動畫，按壓時有縮放回饋

---

## 三、管理後台（系統管理）

管理後台僅限 `is_staff = True` 的管理員帳號存取，提供以下功能：

### 3.1 廣告投放管理

| 功能 | 說明 |
|------|------|
| 新增廣告 | 上傳圖片 + 填寫標題 + 連結（選填） |
| 瀏覽廣告 | 已上架廣告列表（含圖片預覽） |
| 刪除廣告 | 移除指定廣告 |

### 3.2 使用者帳戶管理

管理員可在「帳戶剔除區」查看所有非管理員使用者的詳細資訊：

| 欄位 | 說明 |
|------|------|
| 校園 ID（學號） | 帳號識別碼 |
| 公開暱稱 | 對外顯示名稱 |
| 真實姓名 | 兼容校內 `UserIdentity` 與校外 `OutsiderIdentity` |
| 信箱 | 校內顯示學校信箱、校外顯示聯絡信箱 |
| 身分類別 | 彩色標籤區分「校內」（綠色）與「校外」（黃色） |
| 註冊時間 | 帳號建立時間 |

- **剔除使用者**：永久刪除帳號及其所有關聯資料（貼文、活動等）

### 3.3 心得回收站

- **已刪除心得列表**：透過浮動按鈕開啟 Modal 查看所有被軟刪除的心得
- **復原心得**：將已刪除的心得恢復為公開狀態
- **永久刪除**：徹底從資料庫中移除心得（不可復原）

---

## 技術架構摘要

| 層級 | 技術 |
|------|------|
| 前端 | React + Vite、React Router、Lucide Icons、CSS Modules |
| 後端 | Django + Django REST Framework |
| 認證 | JWT（SimpleJWT） |
| 資料庫 | SQLite（開發）/ PostgreSQL（生產） |
| 部署 | Render（Free Tier） |
| 樣式 | Glassmorphism 深色主題、Outfit 字體、響應式設計 |
